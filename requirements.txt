python-telegram-bot==21.10
asyncpg==0.30.0
python-dotenv==1.0.1
httpx[socks]==0.27.2

# Web admin panel (bot/services/web_admin.py)
aiohttp==3.10.11

# Optional — only needed if you run the userbot member-list harvester
# (userbot/harvester.py). The main bot runs fine without it.
telethon==1.36.0
